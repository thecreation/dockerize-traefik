http:
##############################################################################
# 中间件设置
##############################################################################
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
    enable-gzip-compression:
      compress: {}